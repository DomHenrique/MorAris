## 1. App List Unification

- [x] 1.1 Wrap the Jazzmin app list loop in a scoping `.gridd-app-list` class in `core/templates/admin/index.html`.
- [x] 1.2 Add CSS to `admin/index.html` (or `custom_admin.css`) to style the app cards (`.card`) to match `.gridd-dashboard-card` (dark background, rounded borders, subtle strokes).

## 2. Action Buttons

- [x] 2.1 Replace the default "Add" and "Change" button text in `admin/index.html` with icons (e.g. `fa-plus`, `fa-pencil-alt` or similar).
- [x] 2.2 Style the new icon buttons to be minimalistic, matching the Gridd aesthetic (pill buttons or subtle background hover effects without heavy outlines).

## 3. Timeline Sidebar

- [x] 3.1 Override the timeline styles (`.timeline`, `.timeline-item`) in `admin/index.html` to remove harsh borders and simplify background colors.
- [x] 3.2 Ensure the timeline text and icons blend seamlessly with the dark theme.

## 4. Typography and Spacing

- [x] 4.1 Apply the `Inter` font globally to the dashboard or specifically to the `.gridd-app-list` and `.timeline` elements via CSS.
- [x] 4.2 Adjust cell padding in the app list tables to increase visual breathing room.
